import styled from "styled-components";

const Box = styled.div`
  padding: 28px;
  background-color: #ffffff;
  border-radius: 12px;
  border: 1px solid rgba(0, 12, 47, 0.06);
  box-shadow: 4px 18px 40px 0px rgba(0, 12, 47, 0.08);
`;

export default Box;
